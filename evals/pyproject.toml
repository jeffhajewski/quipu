[project]
name = "quipu-evals"
version = "0.1.0"
description = "Evaluation harness for Quipu memory"
license = { text = "MIT" }
requires-python = ">=3.10"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
