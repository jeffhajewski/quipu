# Conformance Fixtures

Each fixture contains a JSON-RPC request and the expected response shape for one public method. SDKs, CLI tests, daemon tests, MCP tests, and eval smoke runs should load these fixtures instead of creating incompatible local examples.
