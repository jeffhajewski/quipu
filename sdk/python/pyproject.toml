[project]
name = "quipu-memory"
version = "0.1.0"
description = "Thin Python SDK for Quipu agent memory"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
